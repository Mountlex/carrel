# Convex deployment URL (get this from your Convex dashboard)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex deployment identifier (set automatically by Convex CLI)
CONVEX_DEPLOYMENT=dev:your-deployment

# GitHub OAuth credentials (set in Convex dashboard environment variables)
# AUTH_GITHUB_ID=your-github-client-id
# AUTH_GITHUB_SECRET=your-github-client-secret

# GitLab OAuth credentials (set in Convex dashboard environment variables)
# AUTH_GITLAB_ID=your-gitlab-application-id
# AUTH_GITLAB_SECRET=your-gitlab-application-secret

# Resend API key for email/password authentication (set in Convex dashboard environment variables)
# Required for email verification and password reset functionality
# AUTH_RESEND_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Self-hosted LaTeX compilation service URL
# LATEX_SERVICE_URL=https://your-latex-service.fly.dev

# JWT secret for mobile app token signing (set in Convex dashboard environment variables)
# Generate with: openssl rand -base64 32
# JWT_SECRET=your-jwt-secret
